<div class="flex flex-col gap-3">

    <div class="flex w-[100%] items-center gap-4 min-w-[300px]">
        <span class="font-bold text-slate-200 text-xl">Category</span>
        
        <%=link_to "Add New", new_category_path, data: {turbo_frame: "modal"},class: "text-center ml-auto text-slate-200 underline" %>
        
    </div>

    <% @categories.each do |cat|%>
        <%=render 'categories/links', categories: cat %>
    <%end%>
</div>